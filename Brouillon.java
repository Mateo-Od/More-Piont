package MorePiont;

public class Brouillon {

    /*if (caseValide(caseprov)) {
                // Le joueur joue son coup
                if (i % 2 == 0) { // Le joueur n°1 joue
                    grille[caseprov.x][caseprov.y] = X;
                }   else {  // Le joueur n°2 joue
                    grille[caseprov.x][caseprov.y] = O;
                }
                i++; 
                
            }   else    { // Case non valide (Il faut rechoisir une case)

                    while (!caseValide(caseprov)) { // Tant que la case n'est pas valide
                    
                        System.out.println("Choisissez une ligne comprise dans : (1,2,3).");
                        ligne = input.nextInt();
                        System.out.println("Choisissez une colonne comprise dans : (1,2,3).");
                        colonne = input.nextInt();

                        // Vérification ligne 
                        while (ligne < 1 || ligne > 3) {
                            System.out.println("Erreur : (Ligne)");
                            System.out.println("Veuillez choisir une valeur comprise dans : (1,2,3).");
                            System.out.println("Ligne : ");
                            ligne = input.nextInt();
                        }
                        ligne -= 1;


                        // Vérification colonne
                        while (colonne < 1 || colonne > 3) {
                            System.out.println("Erreur : (Colonne)");
                            System.out.println("Veuillez choisir une valeur comprise dans : (1,2,3).");
                            System.out.println("Colonne : ");
                            colonne = input.nextInt();
                        }
                        colonne -=1;

                        // Crétion de la case avec des valeurs valides
                        caseprov = new Case(ligne, colonne);

                    } // On remonte en haut du while
                    
                // Le joueur joue son coup
                if (i % 2 == 0) { // Le joueur n°1 joue
                    grille[caseprov.x][caseprov.y] = X;
                }   else {  // Le joueur n°2 joue
                    grille[caseprov.x][caseprov.y] = O;
                }
                i++; // On incrémente (Gestion du joueur)

            } */


    
}
